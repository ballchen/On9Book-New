<div class="container">
	<div class='col-sm-2' ng-repeat="book in books">
		{{book.name}}
		<a href="/{{book.id}}">
			<img style="width:100%; height:200px;" src="{{book.images[0].path}}" alt="">
		</a>
		
	</div>
</div>