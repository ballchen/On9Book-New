<div class="container">
	<div class="row">
		<div>書名：{{book.name}}</div>
		<div>作者：{{book.author}}</div>
	</div>
	<div class="col-sm-2" ng-repeat="image in book.images">
		<img style="width:100%; height:200px;" src='{{image.path}}' alt="">


	</div>

</div>